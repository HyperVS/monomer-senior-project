//check if user checked inside or outside roi

package main

//"fmt"
//"log"
//"net/http"

func detectHazardInOutROI() {
	//get information from front end

	//getROI()

	//if user selected inside box and hazard is in box:
	//send an SMS
	//else if user selected outside box and hazard is detected and outside box:
	//send SMS
}
